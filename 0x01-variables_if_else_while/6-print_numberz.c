#include <stdio.h>

/**
 * main - Entry
 * Description: program that prints all single digit numbers of base
 * Return: 0
*/

int main(void)
{
	int a;

	(a = 0);
	while (a < 10)
	if (a < 10)
	putchar(a);
	a++;
	putchar('\n');
	return (0);

}
