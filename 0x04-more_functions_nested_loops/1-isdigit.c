#include "main.h"

/**
 * _isdigit - checks for a digit
 * @c: character to be checked
 * Return: 1 if c is a digit, 0 if otherwise.
 */

int _isdigit(int c)
{
	int c = 0;

	if (c >= '0' && c <= '9'; c++)
	return (1);
	else
	return (0);

}
